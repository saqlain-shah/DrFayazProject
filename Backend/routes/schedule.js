// import express from 'express';
// const router = express.Router();
// import scheduleController from '../controllers/ScheduleTiming/Schedule.js';

// router.route('/')
//     .get(scheduleController.getSchedule) // Add route for GET request to fetch schedules
//     .post(scheduleController.saveSchedule);

// export default router;
